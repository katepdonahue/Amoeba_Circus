%h1 Talents

%p
  - @talents.each do |talent|
    = talent.name
    = link_to "Edit", "/talents/#{talent.id}/edit"
    = link_to "Destroy", "/talents/#{talent.id}/destroy"
    %br

= link_to "New", new_talent_path